# 备忘录 (AGENTS.md)

> 本文件为项目级备忘录：每次对话开始先阅读，将需要长期记住的要点分点记录在此。

## 项目目标
- 实现一个 macOS 应用（位于 Dock），点击 Dock 图标后在图标位置弹出 popover，再次点击 Dock 图标收起。

## 当前任务记录 (2026-08-17)
- 任务：实现 Dock 栏 App，点击 icon 弹 popover，再点收起。✅ 已完成并验证
- **popover 内容改为 Dock 图标竖向列表（2026-08-17 增量）**：PopoverContentView 显示当前 Dock 全部图标，每行仅一个图标（无名称，30pt 居中），ScrollView 可滚动，onAppear 时刷新。
  - **列表倒序排列（上溢出，2026-08-17 增量）**：第一项（如 Finder）在面板**底部贴近 Dock**，越往后越往上排（`ForEach(items.reversed())` + makeListRects 从 popoverRect.minY 起排）；内容超高时**顶部项被裁剪（上溢出）**，ScrollViewReader 初始 scrollTo 第一项（anchor .bottom）让底部项贴 Dock。用户明确："现状是下溢出，我希望是上溢出的"。**图标高分辨率（同增量）**：IconExpansionOverlay.setupLayers 用 `var proposed = CGRect(0,0,256,256)` 取 cgImage 大图，否则默认小表示放大后模糊。
  - **数据源 dockItems()：AX 优先、plist 回退**。AX（dockItemsViaAX）遍历 AXDockItem 的 kAXTitle + kAXURL（URL 桥接为 URL/字符串均可），跳过多余虚拟项；plist（dockItemsViaDockConfig）读 persistent-apps + recent-apps。**坑：磁盘 com.apple.dock.plist 可能缓存不完整（实测用户机器 persistent-apps 仅 1 项 Edge），必须用 AX 才能拿全 Dock 图标**。
  - **popover 宽度跟随图标宽度（已恢复）**：targetIconWidth 在 refreshTarget 记录、animateToTarget 同步 contentSize.width（>0.5pt 才更新），列表随图标宽变窄。
  - **列表图标大小实时 = overlay 大小 + 高度响应式（2026-08-17 增量）**：DockVisualState（**@Observable class** + nonisolated(unsafe) shared 单例，macOS 14 观察机制）——IconOverlay 每帧写 shared.iconSide（= tile宽 × iconContentRatio 0.8），**PopoverManager.animateToTarget 兜底同步**（popover 打开期间用 targetIconWidth 算 iconSide）；PopoverContentView 用 @State 持有 shared，属性变化自动刷新。itemCount + 布局常数（rowSpacing/rowVPadding/contentVPadding）算出 contentHeight，animateToTarget 每帧设 contentSize.height = min(contentHeight, 屏幕顶-锚点y-20)，未到限自适应、到限固定且 ScrollView 溢出滚动。**坑：TimelineView(.animation) 在 NSPopover 中不驱动刷新（图标大小不变），必须用 @Observable 观察**。
  - 授权入口移到设置窗口（SettingsView「辅助功能」Section，启用按钮调 AXIsProcessTrustedWithOptions）。
  - **选择器点击三改进（2026-08-17 增量，用户："给堆叠里面的每一个图标加一个小的 drop shadow""打开的这个判定区也一定要严格，是图标本身，而不是一整个窗口""点击的时候图标要变灰"）**：① **每图标 drop shadow**——setupLayers 建层时 `shadowColor=black / opacity 0.3 / radius 2 / offset (0,-1.5)`（Quartz y 向上，负 y=阴影向下；shadow 跟随内容 alpha 通道=图标形状）；② **打开判定区严格=选中图标自身矩形**——handleSelectorClick 不再"点击选择器窗口任意处都打开"，改为 `anims[idx].layer.frame.contains(anchorLayer.convert(mouse, from: container.layer))` 命中才 openApp，未命中（点在灰底/空白）无动作；③ **点击变灰反馈**——flashPressedFeedback(on:)：在图标层加**子层**灰色遮罩（white 0.3 alpha 0.55、圆角 bounds.width×0.2237、frame=iconLayer.bounds，随图标移动不脱离），保持 0.1s 后 0.25s 淡出移除（CABasicAnimation opacity，isRemovedOnCompletion=false）；handleSelectorClick 与展开态 handleClick 均调用。坑：遮罩必须做图标**子层**（frame=图标 bounds）而非 anchorLayer 子层，否则退出选择器/收起动画时遮罩留在原位脱离图标。
  - **点击误开修复（2026-08-17 增量，用户："我点击图标以外的灰色窗口内的区域就会直接打开这个图标的窗口"）**：两个原因：① 选择器激活时命中判定用图标 frame（含四角透明区），点图标方形边框角落也算命中 → 改用 **squircle 圆角路径** `NSBezierPath(roundedRect: frame, xRadius: r, yRadius: r)`（r=frame.width×0.2237）做 `contains`，四角透明/灰底不命中；② 折叠态（未 hover 激活）时 selectorWindow 被 orderOut，点击灰底空白**穿透到 Dock**，Dock 打开被灰底盖住的应用 → **selectorWindow 常驻覆盖灰底**（followStacked 末尾统一 `setFrame(selectorZone)` + `orderFrontRegardless`，点击由 SelectorEventView 吞掉，onClick 仅激活且命中图标时打开）；exitSelector / updateSelectorState 移出分支删掉 orderOut。
  - **判定区域调试框（2026-08-17 增量，用户："请你显示一个框显示你的这个判定区域，你首先要保证这个判定区域和你实际的判定区域是一样的"）**：问题仍是"点击图标以外灰色区域误打开"且拦截无效——用户指出根因是**绝对定位偏移**（事件窗口按 selectorZone 摆放若与灰底不重合，点击灰底就穿透 Dock）。加两个调试框（纯视觉，container.layer 顶层子层，不影响事件）：**红框 = selectorZone**（hover 激活 + 事件窗口实际覆盖区）、**蓝框 = 选中图标 frame 转屏幕坐标**（点击打开实际命中区，与 handleSelectorClick 同一 frame）。**关键：显示数据与判定使用同一来源**（红框直接 = selectorZone 计算属性、蓝框直接 = anims[idx].layer.frame 经 container.convert(from: anchorLayer)），绝不另算一套，保证"显示=实际"。updateDebugFrames() 在 followStacked / followSelector 末尾每帧调用；setupLayers 末尾把两个框 addSublayer 到 container.layer 顶层（anchorLayer 之上）。
  - **判定区域三修正（2026-08-17 增量，用户反馈）**：① **蓝框/命中区缩小**——蓝框（= 点击打开命中区）位置正确但略大于可见图标（AX 图标图像在 frame 内留白边）→ 新增 `iconHitRect(frame)` = frame 内缩 8%，handleSelectorClick 与蓝框共用（显示=实际）；蓝框改 `cornerRadius = hit.width×0.2237` 边框随圆角走，贴合 squircle 命中形状。② **Dock 隐藏时事件窗口必须同步隐藏**——followStacked 的 iconRect==nil 分支（Dock autohide 隐藏）加 `selectorWindow.orderOut(nil)` + 两个调试框 isHidden=true，否则灰底区域仍拦截点击干扰正常使用（用户："你都可以隐藏了，这个应该不拦截了"）。③ **打开应用后抢回焦点**——点击打开后焦点被目标窗口抢走但鼠标仍悬停灰底上：updateSelectorState 的 suppressSelectorUntilExit 分支中 `inside && !NSApp.isActive` → `NSApp.activate(ignoringOtherApps: true)`（用户："只要你这个鼠标是在灰色矩形内的，你就要把这个焦点抢回来"）。坑：didBecomeActive 只调 updateAnchorFromMouse，无弹面板/展开副作用，activate 安全。
  - **Dock 隐藏拦截 + 打开后保持悬浮（2026-08-17 增量，用户："Dock 隐藏了以后仍然乱拦截点击事件""点击打开应用以后，就算我的鼠标还在灰色框内，整个窗口会被重置为折叠台，而不是悬浮台"）**：① **Dock 隐藏仍拦截的根因**——updateSelectorState 的 inside 分支走 followSelector()，而 followSelector 不检查 iconRect：Dock autohide 隐藏后鼠标若停在 currentStackedRect（最后已知位置）区域，inside 判定仍为 true → selectorWindow 被再次拉起拦截。修复：updateSelectorState 开头 `guard DockVisualState.shared.iconRect != nil else { followStacked(); return }`（Dock 隐藏 → followStacked 占位 + orderOut selectorWindow + 隐藏调试框）。② **打开应用后保持悬浮**——删除 suppressSelectorUntilExit 机制（此前"点击后抑制 hover 直到移出"）与 exitSelector()：handleSelectorClick 打开应用后不再折叠，选择器是否激活完全由 updateSelectorState 的 inside/else 管理——鼠标仍在判定区域内则继续悬浮（可连续滚动/点击多个图标），移出才折叠。③ **焦点抢回移入 inside 分支**（通用，不再依赖 suppress）：`!NSApp.isActive && CACurrentMediaTime()-lastFocusStealTime > 0.3` → NSApp.activate，0.3s 节流防每帧重复 activate。
  - **灰底列表自定义 + 设置窗口（2026-08-17 增量，用户："去掉所有的 debug 框""在灰色矩形内部的右上角添加修改图标，点击后设置灰底显示的图标列表，可选择正在运行的程序在灰底显示，并在 Dock 上隐藏它"）**：① **删除全部 debug 框**（zoneDebugLayer/iconDebugLayer/updateDebugFrames 及调用）。② **编辑图标**：editIconLayer（白色齿轮 gearshape，anchorLayer 子层、局部坐标），updateEditIcon(stackedLocal:) 定位在灰底右上角内侧（toAnchorFrame 后 x∈[0,宽]、y∈[0,anchorSide]，size=anchorSide×0.34、margin 5），followStacked/followSelector 每帧调用，Dock 隐藏时隐藏；handleSelectorClick 先判 `editIconLayer.frame.insetBy(dx:-4,dy:-4).contains(mouse)` → 打开设置窗口。③ **数据源改为配置驱动**：DockListConfig.barItems()——`bar.enabledIds`（有序，空=默认全部 Dock AX 图标）非空则按 id 解析显示；prepareStacked/expand/collapse 全部改用它；新增 `refreshList()`=prepareStacked() 供设置变更后重建。④ **设置窗口**：DockListSettingsWindow（居中独立 NSWindow + NSTableView，行=BarAppCellView：图标+名称+「灰底显示」+「Dock 隐藏」两个 checkbox）；candidates() = Dock 当前图标(AX) + 运行中应用（按 path/bundleID 去重）；「灰底显示」即时 setShowInBar+refreshList；「Dock 隐藏」即时从 persistent-apps 移除（DockSpacerRegistrar.removeFromPersistentApps(paths:) 按 _CFURLString 匹配，保留自身+spacer）+ killall Dock，**本 app 禁止隐藏**（自身是灰底锚点）。⑤ 焦点抢回加 `!DockListSettingsWindow.shared.isOpen`（设置窗口打开时不抢焦点）。
  - **设置列表排序 + Dock 隐藏限制（2026-08-17 增量，用户："你应该优先显示现在正在运行的台前程序""就算你选了Dock隐藏这个程序也没有在Dock里面隐藏"）**：① candidates() **排序：台前应用(rank0) > 其他运行中应用(rank1) > 非运行固定应用(rank2)**（组内按名称；rank 用 path 或 bundleID 匹配 runningApps，台前用 isActive/frontmostApplication）；② **「Dock 隐藏」对运行中应用无效（macOS 硬限制）**：运行中应用的 Dock 图标在系统管理运行区，不在 persistent-apps 里，无法移除；即使从 persistent-apps 移除，运行中也会重新出现在运行区。处理：设置窗口对运行中应用**禁用「Dock 隐藏」checkbox + tooltip 说明**（DockListConfig.isRunning 判定），只有非运行的固定 Dock 应用（persistent-apps 条目）能真正移除（DockSpacerRegistrar.removeFromPersistentApps + killall Dock）。注意：用户机器 persistent-apps 几乎为空（仅 DockPopover+spacer），能隐藏的固定应用很少。
  - **Dock 隐藏重构为可靠同步系统（2026-08-17 增量，用户："还是不稳定，有的时候成功有的时候不成功，有的时候选了隐藏实际也没有隐藏，有的时候又相反，严格计划一个系统保障可靠性和功能性"）**：根因：① 取消隐藏没有把应用**加回 Dock**（只删不恢复 → "相反"）；② 无验证/重试，Dock 退出时可能覆写 plist；③ 无启动补漏。重构：**期望状态 = `bar.hideIds`（UserDefaults 单一事实源）→ 幂等同步到 Dock**。① `hideOnDock`：从持久区移除前**保存原始条目+原位置**（`bar.hiddenEntries`），写入后 `verifyRemoved`（读回验证，仍在则重试 3 次）；② `unhideOnDock`：取消勾选时把原始条目**插回原位置**（防重复先删同路径）；③ `applyDockSync()`：**AppDelegate 启动 2s 后调用**，幂等确保 hideIds 应用不在持久区（补漏），仅在变更时 killall Dock；④ 匹配用 `normalizedPath`（去 file:// 前缀、百分号解码、去尾部斜杠）精确相等，不再用 hasPrefix（防误匹配）；⑤ **Finder 禁止隐藏**（系统必备，path 含 Finder.app）；⑥ DockSpacerRegistrar 暴露 `persistentAppEntries()/setPersistentAppEntries()/restartDockNow()/fileURLString(of:)`（原 removeFromPersistentApps 已删）。坑：运行区应用仍无法隐藏（见上条记录，UI 已禁用）。
  - **灰底无法渲染修复（2026-08-17 增量，用户："现在灰色矩形无法渲染，包括图标也是"）**：根因——`barItems()` 用 `bar.enabledIds` 里的 id 去 `candidates()` 查找，而 candidates() 只含 **Dock AX 图标 + 运行中应用**；用户配置了已从 Dock 隐藏且未运行的应用（如 bundleID `com.microsoft.edgemac.helper`）→ 查不到 → 列表为空 → `prepareStacked` guard 失败 → 灰底/图标全不渲染。修复：① `barItems()` 候选缺失时按路径/bundleID 直接解析（`resolvePath` + `NSWorkspace.icon(forFile:)`，已安装即显示）；② `resolvePath` 增强：bundleID 先查运行中应用，再 `urlForApplication(withBundleIdentifier:)` 解析未运行已安装应用；③ 空列表兜底回退默认 Dock 列表（保证始终可渲染）。坑：配置 id 生命周期——勾选时存的是"当时的 id"（Dock AX 存 path、运行中存 bundleID），应用隐藏/关闭后必须能从已安装路径重新解析，不能只依赖运行态。
  - **状态徽标修正 + 运行中可配置隐藏（2026-08-17 增量，用户："你的在 dock badge 覆盖不全""在运行的也可以配置，但是你要追踪，并提示重启生效"）**：① **badge 覆盖修正**——运行中应用在 Dock 运行区可见：未隐藏显示「在 Dock」(绿)、已配置隐藏显示「待重启生效」(橙)；非运行：配置隐藏且已移除→「Dock 隐藏中」(灰)、配置隐藏但还在→「未同步」(橙)、否则「在 Dock」(绿)。② **运行中应用允许配置「Dock 隐藏」**（不再禁用，仅 DockPopover 自身仍禁用）：勾选即写入 hideIds 追踪（持久化）；运行中图标在运行区、退出后才消失，故 hideCheck/badge toolTip 提示"应用退出后自动隐藏生效"（badge=待重启生效）。
- 技术选型：Swift 6 + SwiftUI + AppKit（NSPopover），SPM 构建 + 打包 .app，ad-hoc 签名。
- **移除「Dock 隐藏」功能（2026-08-17 增量，用户："dock 隐藏就不要了，我们能够决定的只有在不在灰底显示"）**：**删掉整个 Dock 隐藏系统**——我们唯一能决定的属性只有「在不在灰底显示」（添加/移除），Dock 显隐不再由本 app 管理。删除：DockListConfig 的 hideIds/hideIdsKey/hiddenEntriesKey/setHidden/hideOnDock/unhideOnDock/verifyRemoved/applyDockSync/saveHiddenEntry/takeHiddenEntry/isHiddenConfigured/entry(matches:)（applyDockSync 的 AppDelegate 调用同步删）；DockSpacerRegistrar 的 setPersistentAppEntries/restartDockNow（仅保留 persistentAppEntries/fileURLString 供位置判定）；设置窗口 ActiveRow.hideConfigured、toggleHide、ActiveCellView 的 hideCheck 与隐藏类徽标（Dock 隐藏中/待重启生效/未同步）。**徽标简化为**：状态——「运行中」(蓝)、「在 Dock/不在 Dock」(绿/灰，isOnPersistent 或运行中非 agent)；属性——「菜单栏」(青)。用户旧配置 bar.hideIds/bar.hiddenEntries 残留 UserDefaults 不再读取。
- **徽标规范：多徽标并存 + 属性/状态分离（2026-08-17 增量，用户："badge 是可以多个的，就是为了精准的显示状态，运行，位置等都要有，然后属性比如说在不在 dock 显示这种是属性，是静态的……我们能改的就是部分 badge，比如说显示在灰底这种"）**：Active 列表每行改为**多个徽标并存**（badgeStack），分两类：**状态（只读，动态）**——「运行中」(蓝)、位置（互斥）：在 Dock(绿，persistent 存在或运行中非 agent)/ Dock 隐藏中(灰，配置隐藏且已移除且未运行)/ 待重启生效(橙，配置隐藏且运行中)/ 未同步(橙，配置隐藏但 persistent 还在)/ 不在 Dock(灰)；**属性（静态）**——「菜单栏」(青，isAgent 应用无 Dock 图标)。**可修改的属性 = 「Dock 隐藏」开关（checkbox）+「灰底显示」（移除按钮/添加）**；状态徽标只读。位置判定新增 DockListConfig.isOnPersistent(path:)（persistent 存在）+ isAgent(id:path:)（暴露 isAgentApp）；删除不再使用的 isActuallyHidden 与 ActiveRow.actuallyHidden。注意：运行中且配置隐藏 = 运行区图标仍在（Dock 可见），徽标为「运行中」+「待重启生效」并存——如实反映"进程在跑、Dock 隐藏要等退出生效"。
- **菜单栏应用启动器 + 多 Profile（2026-08-17 增量，用户："菜单栏应用启动器吧，这些应用没有 dock 图标，这样可以省下来菜单栏空间，然后添加多 profile 能力"）**：① **菜单栏应用识别**——Candidate 加 `isAgent`（LSUIElement / LSBackgroundOnly == true，无 Dock 图标），candidates()/allInstalledCandidates() 用 `isAgentApp(at:)`（Bundle infoDictionary 读取）标记；「添加」候选窗口里 agent 应用显示青色「菜单栏」徽标（AddCellView.agentBadge），便于识别可省菜单栏空间的工具。② **多 Profile**——DockListConfig：`bar.profiles`（名称数组，至少一个「默认」）+ `bar.currentProfile`；enabledIds 改为**按当前 profile 存取**（键 `bar.enabledIds.<profile>`），默认 profile 读取时兼容旧键 `bar.enabledIds`（迁移）；addProfile/removeProfile（至少保留一个，删当前切第一个并 refreshList）/switchProfile（切换即 IconExpansionOverlay.refreshList）；**hideIds 保持全局**（Dock 隐藏不随 profile 变）。设置窗口顶部加 profileBar：NSPopUpButton（当前 profile）+「＋新建」（NSAlert 输入名称）+「删除」；切换/增删后 reload + refreshProfileBar。使用场景：一个 Dock 图标位的灰底按"工作/开发/演示"切换，菜单栏工具按需从灰底启动省菜单栏空间。
- **设置窗口重构为 Active 列表（2026-08-17 增量，用户："整体应该就是一个 active 的列表，要修改再弹出来增减""清晰展示配置和实际情况""只改配置页面"）**：DockListSettingsWindow 重写——**主视图 = 灰底当前显示的应用列表**（有序，来自 barItems()），每行：图标+名称+**状态徽标**+「Dock 隐藏」开关+移除(−)；底部「+ 添加应用」→ 弹出候选窗口（allInstalledCandidates：Dock 图标+运行中+已安装，过滤已勾选），点「加入」即加入并关闭。**状态徽标（配置 vs 实际）**：运行中(蓝，运行区无法隐藏) / Dock 隐藏中(灰，hideConfigured 且 isActuallyHidden) / 未同步(橙，hideConfigured 但 persistent-apps 还在) / 在 Dock(绿)。每 2s Timer reload 刷新状态。移除 = removeFromBar（取消灰底显示 + 若曾隐藏一并取消隐藏，完全恢复）。新增 DockListConfig API：isEnabled/isRunningID/isHiddenConfigured/isActuallyHidden/removeFromBar/allInstalledCandidates（枚举 /Applications、~/Applications、/System/Applications，Bundle 读 bundleID 去重）。
- **设置列表勾选项置顶（2026-08-17 增量，用户："最上面应该有几个正在灰底显示的 active 项，包括隐藏的 active 项"）**：candidates() 排序再加"是否已勾选灰底显示"维度置顶（isEnabled 匹配：id==c.id / id==c.path / id==c.bundleID 任一命中即算，兼容 path/bundleID 两种 id 表示），其中包含从 Dock 隐藏但仍配置显示的项；组内再按原 台前>运行中>非运行 排序。
- **设置列表点减号无反应修复（2026-08-18 增量，用户："点减号无反应"）**：**根因**——当前 profile 的 `enabledIds` 为空 → 灰底处于**默认模式**（空=全部 Dock AX 图标），设置列表里的行都不是配置项，`removeFromBar` 对空配置是 no-op → 点减号看似无反应。**修复①**：`removeFromBar` 默认模式下先把当前默认列表（`dockIconsWithRects`，id=path ?? title）**物化**为 enabledIds 再移除目标 → 减号永远生效（移除一个 = 进入定制模式、灰底变"全部减去该应用"）。**修复②**：新增 per-profile `isCustomized`（键 `bar.customized.<profile>`，默认 profile 迁移兼容旧 `bar.enabledIds` 非空视为已定制）区分"从未定制（空=默认全部 Dock 图标）"与"主动清空（空=空列表）"——移除最后一个应用后不再回退成全部 Dock 图标（否则设置列表看起来毫无变化）；`setShowInBar(on:true)` 置 true；`removeProfile` 清理对应键。**修复③**：`removeRow` 补 `IconExpansionOverlay.shared.refreshList()`（此前缺失，移除后灰底不同步刷新）。**修复④**：`barItems()` 默认分支 id 统一为 `path ?? title`（与 candidates/配置模式一致）——否则默认模式设置行 id 是 title，物化后是 path，`removeAll` 匹配不上。**修复⑤**：removeBtn（无边框 image-only NSButton）加 24×24 约束 + required hugging/compression resistance，防 NSStackView 压缩把命中区域挤没。坑：`DockItem.id` 注释写 bundleID 实为"title 或 path"，不同来源 id 语义不一致是多次 bug 之源。
- **同一应用多种 id 形态导致的"在 Dock 判定"错误（2026-08-18 增量，用户："丁丁是在dock的但是你却显示不在"）**：**根因**——`enabledIds` 里同一应用残留多种 id 形态：路径（`/Applications/DingTalk.app`）、bundleID、**裸 title**（`DingTalk`）。裸 title 来自 AX 取不到路径时 candidates 用 `l.path ?? l.item.name` 作 id；该条目 path=nil、id 既非路径也非 bundleID → `isRunningID` 匹配失败 → 运行中的钉钉误判"不在 Dock"（实测钉钉 bundleID 是 `5ZSL2CJU2T.com.dingtalk.mac`，与 title 不相等）。**修复（统一三种 id 形态的匹配）**：① `isRunningID` 增加 `localizedName == id` 匹配；② `resolvePath` 增加"裸 title → 运行中同名应用 bundleURL.path"兜底（title 形态可解析）；③ `barItems()` 配置分支按 `normalizedPath` **去重**（同应用多形态 id 不再显示两行）；④ `isOnPersistent(path:id:)` 改为 path / bundle-identifier / file-label 三形态匹配（兼容裸 title 条目，签名加 id 参数）；⑤ `removeFromBar` 移除**该应用全部 id 形态**（按解析路径比较，防残留重复行）；⑥ `candidates()` Dock 布局循环：AX 无路径时优先用运行中同名应用的 bundleID 作 id（**不再产生裸 title**）；`isEnabled` 增加 `id == c.name` 匹配。坑：id 三形态（path/bundleID/title）是配置一致性问题之源，判定/解析/去重一律按"解析后路径"归一。
- **菜单栏图标功能放弃（2026-08-18 调研，用户原要"菜单栏应用显示菜单栏图标"，后放弃改做边缘模式）**：调研 Ice（`MenuBarItemImageCache` + ScreenCapture）、MenuBarOverflow（CGWindowList 截图）均为**按 PID 找 status item 窗口再截图**。**本机 macOS 27 实测：菜单栏 items 没有独立窗口**——全部画进 layer 24 的单个 `Menubar`/`MenuBarAgent` 窗口（`CGWindowListCopyWindowInfo` 枚举无独立 status item 窗口，optionAll 也只见各 app 全宽菜单栏窗口），无公开/私有 API 跨进程枚举 `NSStatusItem` 取 image，AX 拿不到 status item 图像且无法关联 app → **运行时读取"某应用在菜单栏显示的图标"在此系统无稳定 API，放弃**。曾加的 MenuBarIcon.swift（CGWindowList 截图）+ SettingsView 屏幕录制授权入口 + DockListConfig.displayIcon 全部删除（恢复 `NSWorkspace.icon(forFile:)`）。
- **边缘启动器模式（2026-08-18 增量，用户："添加一个单独模式，不依赖于dock，而是屏幕边缘触发"）**：设置「启动模式」= 边缘（与 Dock 模式**互斥**，segmented 切换即时生效），锚定屏幕边缘（AppConfig.edgeDirection：bottom/top/left/right，默认 bottom，设置里 Picker）。**初版 EdgeLauncherOverlay（独立简单图标条）被用户否决（"堆叠样式要保留的呀，所有样式只是变成竖向的，不是让你重写一个"）→ 改为改造 IconExpansionOverlay 方向参数化（复用同一套堆叠/选择器样式）**。**实现**：① `layoutAxis`（.x/.y，边缘 left/right 竖向、其余横向）；② `updateAnchor()` 无参化——dock 锚点 = Dock 图标中心（scale=iconRect.width/anchorSide），edge 锚点 = 屏幕边缘内侧 anchorSide/2（**保证灰底完全在屏内**，scale=1）；③ 布局函数 axis 化：makeStackedRectsLocal（竖向 y 错位）、stackedRectLocal（竖向 width=anchorSide/height=...）、selectorRects（竖向 lift 沿 x 向屏幕内，left=+x/right=-x，逻辑排列沿 y，内侧边对齐）、updateWindowMask（竖向向屏幕内开放）、updateEditIcon（right 边缘放灰底 minX 端）、selectorZone（top 向下/left 向右/right 向左延伸，dock 与 bottom 向上）；④ followStacked/followSelector/updateSelectorState/prepareStacked 的 `guard iconRect` 均改为 `!isEdgeMode && iconRect==nil`（边缘模式不依赖 Dock）；⑤ SelectorEventView 加 `vertical`（竖向取 deltaY，横向取 deltaX）；⑥ makeStackedRectsLocal maxOffset：edge 不 clamp spacerWidth。**删除 EdgeLauncherOverlay.swift**；AppDelegate.applyLaunchMode = refreshList()；设置方向切换也 refreshList。坑：初版"边缘无效果"双根因——init 里 `container.layer` 为 nil 导致图层从未挂载（改 start 后 attachLayers）+ `NSScreen.main` 无 key window 时返回 nil（改 screens.first）；displayLink 依赖窗口可见性（边缘模式改用 Timer 轮询兜底，后并入主 overlay 无此问题）。
  - **边缘模式图标屏幕边缘对齐修正（2026-08-18 增量，用户："图标位置对齐应该是屏幕边缘对齐，现在右侧的是做对齐""我指的是鼠标 hover 以后剩余堆叠图标的位置"）**：竖向（边缘 left/right）下 hover 缩放后未选中图标位置不对——原 `selectorRects` 竖向分支 `x = rect.minX + lift` 是 minX 对齐（左对齐），right 边缘下选中项向屏幕内 lift 但**未选中项缩小后右边缘不再贴屏幕右边缘**。修正为**屏幕边缘侧对齐**：right 边缘 `x = rect.maxX - rect.width*scale + lift`（maxX 不变，向内缩 + 选中项向内 lift）；left 边缘 `x = rect.minX + lift`（minX 不变）。连带修正：① `makeStackedRectsLocal` 竖向 jitter 方向根据 edge direction（right 负向屏幕内 -x、left 正向屏幕内 +x），避免奇数项图标超出屏幕边缘；② `stackedRectLocal` 竖向灰底贴屏幕边缘侧（right 时 `x = bounds.maxX - anchorSide` 贴右边缘、left 时 `x = bounds.minX` 贴左边缘），保证灰底与图标都贴屏幕边缘。坑：竖向 right 边缘下原 jitter=+2 让奇数项图标 maxX = f.maxX+2 超出屏幕 2pt，且 selectorRects 缩放后 minX 对齐导致未选中项右边缘内缩 0.35*side 不贴边——必须同时改 jitter 方向 + selectorRects 边缘侧对齐 + stackedRectLocal 灰底贴边三处才彻底对齐。
  - **边缘模式自动隐藏 + 临时屏蔽系统 Dock（2026-08-18 增量，用户："边缘灰底的自动隐藏怎么没了，还有就是可以在设置里临时屏蔽系统dock"）**：① **边缘模式自动隐藏**——`followStacked` 加 `isEdgeMode && !isSelectorActive` 分支：鼠标移出边缘激活区域时**完全隐藏**（window 缩到 16×16、alpha=0、baseLayer/editIconLayer/anim.layer 全 isHidden、selectorWindow orderOut），不依赖 Dock 图标所以不需要常驻。坑：隐藏后 `currentStackedRect=.zero` → `selectorZone` 依赖 `currentStackedRect.width>0` 会返回 .zero → 鼠标到边缘无法重新激活（鸡生蛋）→ `selectorZone` 加分支：边缘模式且 `currentStackedRect` 为空时返回 `edgeTriggerZone(side:)`（屏幕边缘内侧 side×2 窄带，鼠标进入即激活），激活后 `currentStackedRect` 被填充、selectorZone 正常工作。② **临时屏蔽系统 Dock**——设置「系统 Dock」Section 加 Toggle「临时屏蔽系统 Dock」即时生效。`DockSpacerRegistrar.setSystemDockHidden(_:)`：hidden=true 时**先 snapshot 当前用户的 autohide(Int)/autohide-delay(Double)/autohide-time-modifier(Double)** 保存到**本 app UserDefaults**（键 `DockSpacerRegistrar.savedMark/savedAutohide/savedAutohideDelay/savedAutohideModifier`，首次开启才 snapshot，重复开启不覆盖），再写屏蔽值 `autohide=1` + `autohide-delay=10000` + `autohide-time-modifier=1.0`；hidden=false 时**读回保存的三值用 Int/Double 明确类型构造 CFNumber 强制写回**到 com.apple.dock 原生键，无保存过则兜底系统默认，恢复后清除 UserDefaults 标记（下次再开启时重新 snapshot）。`isSystemDockHidden` 读 `autohide-delay>1000` 判定。`AppConfig.ModeKey.systemDockHidden` + `systemDockHidden` 属性持久化用户偏好；AppDelegate 启动 2s 后（spacer 注册之后，避免 killall 冲突）检查并应用，applicationWillTerminate 退出时恢复（防退出后 Dock 仍隐藏）。坑1：单纯 `autohide=1` 鼠标接近边缘仍会唤出 Dock，必须配合 `autohide-delay=10000` 才等于完全屏蔽；用户偏好保留在 UserDefaults，下次启动重新应用。**坑2（用户："屏蔽系统dock以后再打开本来系统dock设置的自动隐藏没了"）**：取消屏蔽时若硬编码恢复成 `autohide=0`（常显），会丢失用户本来自己设置的自动隐藏状态→必须开启屏蔽前 snapshot 三值，取消时完整恢复；savedMarkKey 保证首次开启才保存，避免重复开启覆盖用户最新设置。**坑3（用户："现在dock延迟很大，你临时屏蔽override了延迟也要改回去啊"）**：初版用 CFPreferencesSetAppValue 把原值存在 com.apple.dock plist 自定义键里，savedMarkKey 用 `CFBoolean`，读取时 `as? Bool` 转换失败（CFBoolean ≠ NSNumber）→ 恢复逻辑整个跳过去→屏蔽值 delay=10000 根本没被覆盖→Dock 延迟仍然很大→改用 UserDefaults（原生 Int/Double，无类型转换坑）+ 恢复时用 Swift 侧已解析好的 Int/Double 构造 CFNumber 写回，保证强制覆盖生效。另附带：`autohide-delay` / `autohide-time-modifier` 若用户从未在系统设置中改过，com.apple.dock.plist 根本就不存在这两个键，snapshot 时给系统默认兜底（0.7 / 0.5），避免读取后 as? 返回 nil 再兜底成 0 导致"系统默认被错误覆盖成 0"。
- 关键机制：点击 Dock 图标 → AppDelegate 回调（didFinishLaunching 首弹；didBecomeActive 弹出；applicationShouldHandleReopen 切换显隐；willResignActive 收起）。
- **Dock 图标实时定位（Accessibility API，已实现）**：
  - 遍历 Dock AX 树：AXApplication(Dock) → AXList → [AXDockItem]，用 AXDockItem 的 kAXPosition/kAXSize 实时获取（含 magnification、autohide、拖动）。
  - AX 坐标 = **左上原点、逻辑点**；换算屏幕左下原点：`y = 虚拟桌面总高 - axY - axH`。
  - 匹配本 app：AXDockItem 的 kAXTitle == CFBundleName。
  - 需辅助功能权限（AXIsProcessTrustedWithOptions 弹窗引导）。参考项目：DockDoor / DockLens / Hotdock / ax 工具。
  - 注意：autohide 隐藏时图标坐标在屏幕外，定位结果需过滤（仅当中心在可见屏幕内才采用）。
- Dock 图标锚点：AX 实时定位 > 鼠标点击记录 > plist 估算 > 兜底。锚点 = 图标顶部中心 + iconTopOffset(10pt，与红点共用常量)。
- **popover 实时跟随（lerp）**：**CADisplayLink 驱动**（NSView.displayLink，macOS 14+，preferredFrameRateRange 60-120 支持 ProMotion 高刷），每 3 帧读一次 AX 目标，每帧 lerp（factor 0.45，<0.5pt 吸附）平滑移动。NSPopover 对极小 positioning rect 的水平定位有偏差（~20pt），故用 alignPopoverWindow 手动对齐：popover 窗口左下原点 frame = (anchor.x - w/2, anchor.y)。已验证：0.3s 收敛、最终对齐误差 ~1pt。
- 定位圆点：DotOverlay（14pt 红点，level=.statusBar>dock 20），**CADisplayLink 驱动**（与 Dock 渲染帧同步），每帧读 AX 直接跟随（无 lerp）。最终对齐误差 ~0.02pt。
- **图标覆盖层 IconOverlay（增量功能）**：与图标**位置和大小完全一致**的 overlay，**NSVisualEffectView popover 磨砂材质**（系统最轻模糊，behindWindow + 动态圆角 0.2237、HIG 0.8 比例），CADisplayLink 每帧读 AX 直接跟随（无 lerp，与 Dock 同刷新率），窗口 frame = (center.x-w/2, center.y-h/2, w, h)。已验证对齐误差 <1pt。
- **开关过渡动画 TransitionOverlay（增量功能，不改 IconOverlay）**：**会变形**磨砂 overlay——图标状态：位置/尺寸 = DockVisualState.iconRect（**IconOverlay 每帧维护**，含 overlayVerticalOffset），**仅鼠标 hover 在图标上时显示**（hoverZone = iconRect inset -12pt，不在则不显示、角落占位保持 displayLink）；popover 状态：位置/尺寸 = DockVisualState.popoverRect（**PopoverManager.animateToTarget 每帧维护**，底部中心=anchor、尺寸=contentSize，弹窗可见区域不含阴影），**始终显示**。两状态间 CADisplayLink 0.28s easeInOutCubic 平滑过渡（frame/圆角/alpha 插值）。**架构约定：不重复从底层（locate/window.frame）取数据，一律读共享状态**。show→run(follow:.popover)、close→run(follow:.icon)。图标圆角 = side×0.2237、popover 圆角 12。
- **IconOverlay 常态隐形（AppConfig.iconOverlayVisible=false）**：窗口不显示（角落占位 alpha 0，仅作数据提供者维护 DockVisualState.iconRect/iconSide），代码保留、调试时改开关为 true 恢复显示。
- **Dock 图标展开/收起动画 IconExpansionOverlay（2026-08-17 增量）**：三态（stacked/transitioning/expanded）——**图标状态**：所有图标**堆叠**（ZStack，错位 1pt 形成一摞）在自己 icon 位置（DockVisualState.iconRect），hover 图标时显示否则隐藏；**展开**：堆叠图标从**自己 icon 位置**平铺飞入弹窗区域内竖向列表各自位置（堆叠在磨砂 overlay 之上），CADisplayLink 0.35s easeInOutCubic（位置+尺寸）；**展开完成**：跟随列表位置（每帧按当前 popoverRect/iconSide 重算）。收起从列表飞回堆叠位置。数据源 dockIconsWithRects()（AX tile rect 左下原点）；makeListRects() 与 PopoverContentView 布局常数一致。窗口覆盖整个屏幕，layer frame 用屏幕坐标。**注意：popover 面板不显示时 PopoverContentView.onAppear 不触发，PopoverManager.show() 需自行维护 DockVisualState.itemCount，否则弹窗高度只有占位值 60（变形高度过小）**。
- **popover 面板常态不显示（AppConfig.popoverVisible=false）**：PopoverManager 仍作为信息提供者——show() 不弹面板但启动跟随循环维护 DockVisualState.popoverRect、过渡 overlay 照常变形到弹窗区域；anchorWindow 保持屏内占位驱动 displayLink；close() 按开关决定是否 performClose；isMouseInsidePopover 改用 popoverRect 判断（不依赖窗口）。改开关为 true 恢复弹出面板。
- **非动画状态矩形与图标无相对位移**：DockVisualState.isAnimating（TransitionOverlay 变形 / IconExpansionOverlay 展开收起动画进行中=true，结束=false）。**popoverRect 一律用 iconRect（每帧数据）推导锚点**——anchor = (iconRect.midX, iconRect.maxY - overlayVerticalOffset + iconTopOffset + popoverVerticalOffset)，不依赖 isAnimating/targetAnchor，杜绝 20Hz 目标读取延迟（动画期间矩形/图标各自插值不读该值）。isAnimating 仅控制 PopoverManager 面板 next 是否 lerp。contentSize 更新在 popoverRect 写入之前。
- **横向滚动选择器替代点击展开/收起（2026-08-17 重构）**：用户："不要点击展开或者折叠了"。**AppDelegate.applicationShouldHandleReopen 不再 toggleForDockClick**（点击 Dock 图标只激活 app，无自定义行为）；竖向列表展开/收起（expand/collapse、PopoverManager show/close）代码保留但不再被点击触发。
  - **新交互**：折叠态 hover 横条激活区域（`selectorZone` = currentStackedRect + 上/左/右延伸）→ 选择器激活：**灰底上边缘向上延伸**（extend = side×1.8，底边不变）+ **图标横向弧形走马灯**（selectedIndex 居中=弧顶最高最大，`arc = cos(t·π/2)` 向两侧渐低渐小，maxVisible=5，spacing=side×1.15，lift=side×1.2，minScale=0.65）。
  - **输入**：`SelectorEventView`（独立透明事件窗口，仅激活时覆盖 selectorZone，接收 scrollWheel + mouseDown）——触控板横向滚动**段落感**（scrollAccumulator 累计满 `iconSide×0.9` 才切换 selectedIndex ±1）；点击 → openApp(选中项)（复用 path 打开逻辑）。
  - **退出**：鼠标移出激活区域 → 恢复折叠堆叠；点击打开后 `exitSelector()` 置 suppressSelectorUntilExit 抑制 hover 再激活直到鼠标移出。
  - 选择器激活区域：`CGRect(x: 灰底.minX, y: 灰底.minY−side×0.2, w: 灰底.width, h: 灰底.height+side×2.6)`——**直接以灰底矩形为左右边界**（鼠标绝对位置在矩形内/外判定，零左右延伸；上方延伸选择器弹出区、下方 0.2 side 容差）。**坑：currentStackedRect 转屏幕必须 `container.layer!.convert(toAnchorFrame(stackedLocal), from: anchorLayer)`**——直接 convert(stackedLocal) 少 +anchorSide/2 偏移，判定区域整体偏左（用户："整个判定区域往左偏移了"）。此前 side×2 / side×0.6 / 不对称延伸都嫌左侧偏多（用户："左侧还是偏多，就不能用绝对位置吗"）。
  - **坑：followSelector 必须恢复全屏窗口**（用户："鼠标在灰色矩形区域内唤醒 dock 反而所有都不会显示"）——Dock autohide 唤出动画期间 iconRect 短暂 nil，followStacked 把窗口缩到 16×16 角落占位，恢复后鼠标在激活区直接走 followSelector（不经过 followStacked 的恢复逻辑）→ layer 在窗口外被裁剪全不显示。修复：followSelector 开头同 followStacked 判断 `window.frame != screenFrame` 则设回全屏。
  - **跟随层架构（2026-08-17 重构）**：用户："所有元素跟随默认图标的位置和缩放""加个定位层、局部坐标系"。新增 **anchorLayer**（跟随层）：position = 图标中心（屏幕）、transform = 当前 side/基准 side 缩放；**灰底与所有图标作为其子层，frame 用局部坐标（图标中心为原点、基准尺寸 anchorSide = tile-size×0.8）**——图标移动/放大时整层自动继承，杜绝各元素绝对定位的错位/滞后。布局函数改局部：`makeStackedRectsLocal`（不乘 scale，spacer/错位用基准值）、`stackedRectLocal`、`arcRect(at:side:)`（灰底中心 y=extend/2）、`renderBase(stackedLocal:)`（x/y/宽直接，高按 progress lerp）、`toAnchorFrame`（局部→anchor 坐标 = +anchorSide/2）。屏幕坐标转换：局部→屏幕用 `container.layer!.convert(toAnchorFrame(local), from: anchorLayer)`（currentStackedRect/stackedBounds，**必须先 toAnchorFrame，直接 convert 少 +anchorSide/2 偏左**）；屏幕→局部用 `anchorLayer.convert(点, from: container.layer)`（handleClick 命中）。expand/collapse 保留兼容（不触发）。
  - **选择器滚动弹性反馈（2026-08-17 增量，用户："横向滚动一直太大了""就算没到阈值也应该有弹性的动画，像 iOS 模态窗一样有阻力，不是完全没有反馈"）**：① 灵敏度再调低——`handleSelectorScroll` sensitivity **0.015→0.009**，边界橡皮筋压缩 **0.25→0.2**；② **非精密滚轮归一化**（`SelectorEventView.scrollWheel`）：`hasPreciseScrollingDeltas=false`（鼠标滚轮/平滑滚动）时 delta ×10 换算成点再交给灵敏度，否则滚轮一格滚太远；③ **停止后弹性回弹**：原指数 lerp（`scrollOffset += (nearest-offset)×0.2`，迟缓"无反馈"）改为**带初速度的阻尼弹簧**——`scrollVelocity`（滚动事件按单位时间位置变化量估算，作为弹簧初速度，实现惯性滑行）、`springStiffness=260`、`springDamping=15`（阻尼比 ~0.45，轻微过冲体现弹性），`tick` 的 frameDelta 作为弹簧积分步长（clamp ≤1/30 防帧率跳变），收敛判据 `|nearest-offset|<0.01 && |velocity|<1` 后吸附；**未到切换阈值也持续有动画**（offset 连续变化 → 高斯 lift 连续运动 + 停止后回弹）。**④ 回弹要及时（同轮修订，用户："最后的 snap 会在我没有操作以后很久才弹回去，应该是及时的"）**：`springStiffness 260→1400`、`springDamping 15→48`（阻尼比 ~0.64，几乎无过冲，收敛 ~0.15s）；`scrollVelocity` 钳制 ±`maxScrollVelocity=8`（图标/秒，防突发 delta 滑行过远）；isScrolling 判定窗口 **0.12→0.08s**。**⑤ 重写为"拖动弹簧跟随"模型（同轮修订，用户："横向完全平滑没有阻力，应该滚动时就能感到弹性"——用户要求查 Apple 文档/教程：NSScrollView.Elasticity、WWDC803 Designing Fluid Interfaces、How UIScrollView Works 橡皮筋公式、Flutter 对 UIScrollView 回弹曲线的实测）**：拆分为 `rawOffset`（手指原始位置，直接累计输入 + **边界用 iOS `rubberBandClamp` 公式** `(1−1/(over×0.55+1))`，越拖越吃力有极限）+ `scrollOffset`（内容显示位置，**两阶段不同弹簧**）：滚动中目标=rawOffset、软弹簧 `followStiffness=350/followDamping=24`（阻尼比~0.64）→ 内容滞后手指有张力，**滚动时即感弹性**；停止后目标=最近图标、硬弹簧 `snapStiffness=1500/snapDamping=60`（近临界阻尼，收敛~0.13s）→ 及时吸附。速度钳制 ±10。坑：退出/进入选择器、鼠标移出时 rawOffset/scrollVelocity 须一并清零。**⑥ 改为"抽屉断点"模型（同轮修订，用户："和刚刚没有区别，你去看看抽屉的断点是怎么实现的，抽屉的断点和这个是一个道理"——抽屉= iOS bottom sheet/detent，原理：拖动 1:1 直跟手指，松手瞬间弹簧吸附到最近断点，吸附本身是可见弹性动画）**：**删除 follow 弹簧**（之前"滚动中内容滞后手指"造成与旧版无感知差异）——滚动中 `scrollOffset = rawOffset` **1:1 直跟不滞后**；松手判定 `stopWindow = 0.06s`（2-4 帧，无感知延迟）后立即用**吸附弹簧** `snapStiffness=1000/snapDamping=36`（ζ≈0.57 欠阻尼，快速+轻微过冲回弹，收敛~0.2s）向最近图标弹回——即使没到切换阈值也有可见弹性动画（像抽屉拖一点点松手弹回原位）；`scrollVelocity` 改由滚动事件估算（本次移动量/事件间隔）作为弹簧初速度（甩动惯性滑行），钳制 ±10；边界仍用 rubberBandClamp。**⑦ 改为"snap 点磁吸"模型（同轮修订，用户："太不敏感了""首先你要定位一个位置，这个位置是 snap 点，然后每一个图标都有这个位置，就这个位置是图标最高的时候，滚动的时候远离这个点的时候，高斯函数实际移动的位置的和实际你在触控板上的移动的位置的这个系数越来越小，给人一种这个地方很难跨过去的感觉"）**：每个图标一个 snap 点（= 该图标高斯最高位，整数位）；`rawOffset` 只累计手指输入（**不再做任何压缩**，灵敏度 0.009→**0.012**，用户嫌 0.015 太大/0.009 不敏感）；每帧目标 `magnetTarget(finger) = 最近snap + 压缩位移`，压缩 `compressed(d)=d·(1−0.8·|d|)`：d=0 斜率 1（跟手 1:1），|d|→0.5 斜率递减到 0.2（**内容位移/手指位移系数越来越小 → 很难跨过去**），跨过中点最近 snap 切换、目标跳到下一图标（咔哒感，弹簧平滑），|d|>0.5（边界外）压缩封顶 0.3；松手目标 `snapTarget = round(finger)` 钳制（**阈值=中点**），内容统一弹簧 `snapStiffness=800/snapDamping=34`（ζ≈0.6，跟手不滞后+松手可见回弹）。删掉 rubberBandClamp（磁吸已涵盖边界阻力）。**⑧ 磁吸调强 + 惯性说明（同轮修订，用户："告诉我手的一次滚动是有什么样的数据，因为是有惯性的，你要把惯性也算进去，然后现在这个越来越难，还是有一点不明显"）**：① 压缩曲线 `d·(1−0.8·|d|)` → `d·(1−|d|)`（**中点斜率降到 0、峰值最多离 snap 点 0.25**，"很难跨过去"更明显；边界封顶 0.3→0.25）；② **惯性已经天然算进去**：触控板一次横滑 = 手指拖动段（`phase .began→.changed×N→.ended`，`scrollingDeltaX` 每帧 0.5~20pt 点单位）+ 惯性段（抬指后 `momentumPhase .began→.changed×N→.ended`，delta 指数衰减约 0.5~1.5s）——两段事件全部累计进 rawOffset，`isScrolling` 只要有事件就保持，惯性结束 0.06s 后才切吸附弹簧，所以抬指后内容继续滑动、惯性够强可跨断点。**⑨ 重写为"牛顿物理建模"（同轮修订，用户："为什么 snap 是两段式的应该是一段式的""我们要数学建模：高斯函数有质量，滚动每帧一个力，不在 snap 点 snap 也会给一个力，合力/质量=加速度更新下一帧位置""滚动进行时阻力也实时有效，不是一整次滚动完了才更新"）**：**删除全部两段逻辑**（rawOffset / isScrolling / stopWindow / magnetTarget / snapTarget）——一套统一物理 `integratePhysics(dt)` 每帧积分：① 输入=滚动事件累计成**速度冲量** `pendingImpulse += delta×0.012`，每帧合并进 v（等效输入力×dt/m）；② snap 点**弹簧力** `F=-k·(x−round(x))`，距 snap 越远拉力越大 → 滚动中**实时**阻力（越推越难）、越过中点自动换 snap；③ 粘性阻尼 `2ζω·v`；④ `a=(F_snap−c·v)/m`，`v+=a·dt, x+=v·dt`。松手后输入力消失只剩 snap 拉力+阻尼 → **自然一段式回弹落位**（惯性天然保留）。参数 mass=1、snapStiffness=400（ω=20）、dampingRatio=0.6、maxScrollVelocity=10；收敛判据 |x−snap|<0.02 且 |v|<0.5 吸附。
  - **选择器基础版：横向/大小完全不动，仅垂直按高斯（2026-08-17 重构）**：用户："不需要去动图标的大小横向的位置""垂直位置按高斯""只有被高斯函数影响到的图标才有变化，其他保持原样"。`selectorRects(count:stacked:side:)`：图标**横向位置/大小完全保持堆叠位置**（stacked），只在堆叠位置基础上垂直偏移 `lift = 1.2side × exp(-((i−selectedIndex)²)/(2×0.6²))`，峰值在选中项，lift≈0 的图标留在原地。**坑：不能把图标重新均匀分布（用户："所有图标都变高还往左走了"）——基础是"堆叠位置 + 垂直高斯偏移"，其余不动**。退出/过渡仍按 selectorProgress 混合。① followSelector 会 `isHidden=true` 隐藏超出 maxVisible 的图标，**followStacked 必须 `anim.layer.isHidden = false` 恢复**，否则常显横向堆叠消失（用户："常显的横向堆叠有时会消失"）；② **灰底定位必须直接跟图标，不可对整个 frame lerp**（用户："灰色矩形的定位应该与图标一直是相符的"）——引入 `selectorProgress`（0=折叠，1=选择器，每帧 lerp 趋近目标后吸附），`renderBase` 的 **x/y/width 直接 = 折叠矩形（跟图标），仅 height = lerp(折叠高, 折叠高+extend, progress)**；图标目标 = `lerpRect(堆叠rect, 弧形rect, progress)`，折叠态（progress≤0.01）直接赋值堆叠位置（不滞后），选择器态 lerp 平滑滑动（走马灯）。进入/退出选择器时灰底高度、图标展开/收回都随 progress 平滑。**③ prepareStacked 持续重试**（用户："整个灰色都不见了""图标也不在了"，红点因 plist 回退仍在——根因 TCC 授权每次构建失效 + 原重试 5 次后放弃，窗口从未 setup）：改为每 0.5s 无限重试，授权后自动建立堆叠。
- **点击展开列表图标 = Dock 点击行为（2026-08-17 增量）**：展开态下点击列表中的图标 → 打开对应应用并收起（与点击 Dock 图标一致）。实现：DockIconLayout 增 `path: String?`（axDockLayouts 统一解析，运行中应用无路径时用名称激活兜底）；IconExpansionOverlay **全局点击监听**（`ensureClickMonitor` 仅 expanded 态激活、stacked/动画中关闭——窗口覆盖全屏不能取消 ignoresMouseEvents），handleClick 用 `anims[i].layer.frame.contains(mouse)`（屏幕坐标=左下原点，与 NSEvent.mouseLocation 一致）命中检测 → openApp（path 优先 NSWorkspace.open，无 path 用 runningApplications 名称 activate）→ PopoverManager.shared.close() 收起。currentLayouts 在 setupLayers 时更新（setupLayers 增 layouts 参数）。
- **Dock spacer 透明占位（2026-08-17 重构，替代 DockSlot）**：Dock 无法给单图标让位，且 **Dock 对透明 app 图标会强制渲染默认占位图标（淡色），无 API 可关闭**（试过全透明 icns / 小圆点 icns / lsregister / iconservicesagent / 清图标缓存均无效）。最终用 **Dock 原生 spacer-tile（透明空白分隔符）** 占位：`{"tile-data": {}, "tile-type": "spacer-tile"}` 插入 persistent-apps 中 DockPopover 条目后；宽度 `spacer-tile-size`（默认 16，代码首次设置 48）。**Dock 排序规则——持久区按 persistent-apps 顺序（Finder 固定第一）、运行区按最近激活 MRU、最后废纸篓；运行中 app（如本 app）默认在运行区位置不固定，必须把 DockPopover 固化为持久条目、spacer 紧跟其后才能相邻**。注册：DockSpacerRegistrar.swift 用 CFPreferencesSetAppValue 改 com.apple.dock persistent-apps（重建为 DockPopover + spacer 相邻，清理旧 DockSlot 条目，已就位则跳过），然后 killall Dock；AppDelegate 延时 1.5s 调用（避开首次展开）。横条横向延伸 = min(用户设置图标倍数宽, spacer 实际宽度)（spacer 是硬上限，防止盖住右侧真实图标）。
- **灰色 overlay 只加宽（2026-08-17 增量）**：DockVisualState.stackedBounds = 折叠堆叠图标整体包围盒（IconExpansionOverlay.followStacked 每帧写入，Dock 隐藏置 nil）。TransitionOverlay 图标状态（hover 磨砂矩形）改为：有 stackedBounds 时 **只加宽**——`CGRect(x: rect.minX, y: rect.minY, width: bounds.width + 20, height: rect.height)`（左边界/高度/垂直定位保持原 iconRect，向右延伸到堆叠右端留 20pt 余量，堆叠垂直居中）、圆角 12；无则回退 iconRect。**坑：勿用 bounds.insetBy 对称外扩（会左移/加高，用户："没让你改高度和定位，为什么定位往左了"）**。
- **常态灰色底渲染进图标（2026-08-17 增量）**：灰色底由 **IconExpansionOverlay.baseLayer**（CALayer，灰 alpha 0.95、圆角 12、禁用隐式动画）在折叠态常态渲染——从 iconRect.minX 向右延伸到堆叠右端 +20pt（`stackedRect(at:rects:)` 统一公式），盖住 Dock 系统默认图标；堆叠图标叠在灰底之上。**灰底参与展开/收起连续变形（2026-08-17 增量）：展开时从折叠宽矩形插值到 popoverRect（baseFrom/baseTo + transitioning 分支同步插值），展开完成 baseLayer.frame = popoverRect 跟随面板；收起反向变回宽矩形**。setupLayers 清空后重加在最底层。**TransitionOverlay hover 判定保持不动，hover 磨砂矩形尺寸与常态灰底同公式**（有 stackedBounds 时 `(x: rect.minX, y: rect.minY, w: bounds.width+20, h: rect.height)`、圆角 12；无则回退 iconRect、圆角 0.2237）；DockVisualState.stackedBounds 仍是堆叠包围盒（灰底参照）。**用户："magnify 你不用管了"——Dock 设置（magnification 等）不主动恢复/修改。**
- **坑：iconRect 必须"内容 squircle 居中于 tile"，不可"顶对齐 tile"（2026-08-17 修复）**：IconOverlay 此前 `y = tileTopY - side`（rect 顶 = tile 顶），rect 中心在 tile 中心上方 0.1h——正常尺寸看不出来，但 **magnification 放大时 0.1h 偏差线性增大，灰色矩形/堆叠与 Dock 默认图标错位**（用户："灰色矩形和默认图标 magnify 时有位移"）。修复：`rect 中心 = AX center（info.center），y = center.y - side/2 + overlayVerticalOffset`，放大时与 Dock 沿同一中心缩放、无相对位移。所有 iconRect 下游（灰底/堆叠/hover/popover 锚点/展开动画）自动跟随。**连带修复（同轮）**：① TransitionOverlay hover 矩形加宽余量补乘放大系数（`20×scale`，此前仍是旧 `+20` 与灰底不一致，用户："lerp 矩形的最终位置忘记改了"）；② PopoverManager.computeAnchorCenter 初始锚点从"tile 顶（center + h/2）"改为"内容顶（center + side/2）"，与 animateToTarget 每帧锚点一致，消除打开瞬间位置跳变。
- **Dock 硬编码间距/余量乘放大系数（2026-08-17 增量）**：magnification 放大时图标变大但固定偏移不变会很怪。`DockVisualState.magnificationScale` = 当前 tile 宽（iconSide/0.8）÷ Dock 基准 tile-size（com.apple.dock tile-size，DockSpacerRegistrar.tileSize，**nonisolated** 读取），clamp ≥1。乘上的位置：堆叠竖向错位 dy（2pt×scale）、灰底 20pt 余量（stackedRect）、hoverZone inset 12pt（TransitionOverlay）、popover 锚点 iconTopOffset 10pt（PopoverManager 两处）、**maxOffset 的 spacer 硬上限（spacerWidth×scale，避免放大时横条被固定 spacer 卡住不变宽）**。用户手动偏移（overlayVerticalOffset/popoverVerticalOffset）**不乘**。已有比例值（step=side×0.18、圆角 0.2237、maxOffset 用户倍数部分 tile×倍数−side）本身随图标缩放无需乘。
- **坑：IconExpansionOverlay Dock 隐藏恢复后堆叠"消失"（2026-08-17 修复）**：followStacked 在 Dock 隐藏时把窗口缩小到 16×16 角落占位（保持 displayLink），但 Dock 恢复后只更新了图标 layer frame，**忘了把窗口设回全屏**——图标 layer 是屏幕坐标，在 16×16 窗口外被裁剪，堆叠不可见（看起来"变回默认图标"）。修复：记录 screenFrame，followStacked 恢复时 `window.setFrame(screenFrame)` 后再画图层。
- **折叠态替换图标本身（2026-08-17 增量）**：折叠态（未展开）时本 app 的 Dock 图标位置**常态显示横向错位堆叠图标**（IconExpansionOverlay followStacked() 常态显示）——即"图标本身被替换为横向长方形堆叠外观"。makeStackedRects：横向错位**左对齐**（从本图标左边界向右延伸），**最大宽度 = AppConfig.stackedMaxWidth 个图标宽**（设置窗口「折叠外观」Slider 1~3 步长 0.5，默认 2，UserDefaults 键 layout.stackedMaxWidth；maxOffset = min(tile×倍数−side, spacer 宽度)），spacer 是硬上限；竖向轻微错位保留厚度感。**图层顺序：setupLayers 逆序 addSublayer（i 越小越左越在上层）→ 左边图标压右边图标**。**overlay（TransitionOverlay）不常显**：磨砂矩形仍仅鼠标 hover 图标时显示（保留 hoverZone 判定），展开时变形为弹窗区域。**启动即常态堆叠（2026-08-17 增量）：AppDelegate 启动改为 IconExpansionOverlay.prepareStacked()（setupLayers from=to=stacked，phase=.stacked，不播放动画），didBecomeActive 不再自动 show()——展开/收起完全由点击 Dock 图标显式切换，避免"启动默认图标、须先展开再收起才显示堆叠"**。
- **展开状态常态化（2026-08-17 重构）**：展开/收起是显式状态，**不绑定 Dock 可见性**——Dock autohide 收缩时不再自动 close()，矩形/图标保留最后位置，Dock 恢复后继续跟随。refreshTarget 仅当图标真实可见时更新锚点（locatePrecisely + 屏内判断），不可见直接 return（保留 targetAnchor）。**退出条件 = 点击非窗口区域 + 点击 Dock 上本 app 图标**：NSEvent.addGlobalMonitorForEvents 全局监听鼠标按下——点击本 app 图标（isClickingOwnDockIcon，locate() 图标 rect 判断）→ 置 suppressedReopen 标记后 close()（reopen 到来时 toggleForDockClick 消费标记 return，避免 close 后被 toggle 重新 show；show() 开头重置标记防残留）；点击 Dock 条其他区域 → 不退出；点击其他任意位置 → close()。收启动画目标兜底：DockVisualState.lastIconRect（IconOverlay 每帧写入最后已知图标 rect，Dock 隐藏 iconRect=nil 时 close()/collapse() 用此兜底，否则图标飞不回 Dock 位置）。
- **Dock 图标列表统一数据源（2026-08-17 增量）**：axDockLayouts() 统一过滤 + 图标解析——title 非空 且（有可解析路径 或 运行中应用）且 rect 有效；被过滤：recent/Application Windows 等无路径虚拟项。**坑：dockIconsWithRects()（展开动画）此前未过滤虚拟项，无路径项会渲染成问号占位图标（列表 dockItems 却过滤了，两者数量不一致）**。修复：dockItems() 与 dockIconsWithRects() 共用 axDockLayouts()，过滤一致、数量一致。无法解析图标直接跳过该项（不显示问号占位）。
- **Dock 唤出动画期间不误收起（2026-08-17 修复）**：autohide 下鼠标从展开矩形移向屏幕底唤出 Dock，唤出动画几百 ms 内图标不可见，invisibleCount 会累计到阈值误 close()。修复：refreshTarget 三态变为——图标可见（更新锚点）；不可见但鼠标在 popover 内**或位于 Dock 条区域**（isMouseInDockRegion，屏幕 frame 与 visibleFrame 之间那一条，底部/左侧/右侧）→ 保持不收起；否则累计收起。Dock 唤出动画中鼠标就在 Dock 条区域，属于"正在唤出"，不应收起。
- **坑：CALayer frame 赋值有隐式动画（2026-08-17 修复）**：CALayer 修改 frame/position/bounds 默认触发约 0.25s 隐式 action。IconExpansionOverlay 的 displayLink 每帧赋值都会启动新隐式动画 → 渲染永远追不上目标，看起来像非动画状态还在 lerp（用户："一连串 icon 的非动画状态变换还有 lerp"）。**修复：创建 layer 时 `layer.actions = ["position": NSNull(), "bounds": NSNull(), "frame": NSNull()]`**，frame 赋值即时生效，动画阶段完全由代码插值控制。NSWindow.setFrame 无此问题。
- **调试 HUD（2026-08-17 增量）**：DebugHUD.swift 屏幕右下角常驻半透明黑底绿字面板（ignoresMouseEvents + screenSaver level），0.1s 刷新显示 DockVisualState 共享状态：isAnimating / iconSide / itemCount / iconRect / popoverRect / TransitionOverlay.stateDescription / IconExpansionOverlay.stateDescription。AppDelegate 启动即 start()。用途：观察矩形 overlay 相对图标是否延迟，定位延迟来源。
- **坑：overlay 占位尺寸不能太小**：NSVisualEffectView 在 1x1 极小窗口下磨砂渲染异常，Dock 恢复后不重绘（表现为"消失"）。占位须用 ≥16pt（DotOverlay 用 14pt 无此问题），恢复时 boxView.state = .active 强制刷新。
- **popover 在 Dock 隐藏时自动收起**：PopoverManager.refreshTarget 用 DockIconLocator.locatePrecisely()（纯 AX，不回退 plist，plist 估算无法感知 autohide）检测图标是否在可见屏内，连续 dockHiddenThreshold(4 次≈200ms) 不可见 → close()。仅 AX 已授权时检测。
- **鼠标移入 popover 时 Dock 收缩不关窗（借鉴 DockDoor）**：Dock autohide 下鼠标离开 Dock 区域进入 popover → Dock 收缩 → 图标不可见会误关 popover。修复：refreshTarget 三态——图标可见（正常更新锚点/宽度）；图标不可见但**鼠标在 popover 窗口内**（isMouseInsidePopover，window.frame.contains(mouseLocation)）→ 保持原位不收起；否则累计收起。DockDoor 方案是独立 NSWindow + 全局鼠标监听判断面板存续，我们只用鼠标位置判断这一核心点。
- **坑：CGWarpMouseCursorPosition / CGEvent 用左上原点（Quartz 显示坐标）**：nudgeMouseToResetTooltip 若直接用 NSEvent.mouseLocation（左下原点）的 y 会翻转轴，Dock 底部的鼠标会被 warp 到屏幕顶部。换算：`y(左上) = 虚拟桌面总高 - y(左下)`。
- **坑：NSView.displayLink 依赖 view 在屏幕可见区域**，窗口隐藏/移出屏幕会暂停驱动。红点/覆盖层在 dock 隐藏时必须移到**屏内角落占位**（不可 orderOut / 移出屏外），否则 dock 恢复后无法继续跟随。
- **popover 宽度实时跟随图标宽度**：refreshTarget 时记录 icon.size.width，animateToTarget 里同步 popover.contentSize.width（>0.5pt 才更新）。注意 NSPopover 窗口 frame 比 contentSize 宽 ~27pt（阴影边距），contentSize 本身与图标宽精确一致。
- **用户可调竖向偏移（设置，持久化）**：AppConfig.OffsetKey 集中定义 UserDefaults 键（offset.popoverVertical / offset.overlayVertical）；AppConfig.popoverVerticalOffset / overlayVerticalOffset 静态读写。设置入口在 **Cmd+, 设置窗口**（App.swift Settings scene + SettingsView.swift，Form 分组，Slider+TextField，正数向上，范围 ±100）。@AppStorage 绑同键。读取方每帧读静态属性，无需通知机制。popover 内不再放设置项。
- 已知限制：Dock 图标 hover 显示 app 名字标签是系统行为，**无 API 可单独禁用**（需第三方 Dock 修改工具）。
- 关键坑：NSPopover.show(relativeTo:of:) 的 rect 是 view 局部坐标，anchor 窗口 setFrame 到全局坐标后应传 (0,0,1,1)。
- **辅助功能授权（重要）**：TCC 授权与可执行文件哈希绑定，**每次重新构建都会失效**（固定证书也无效）。因此**启动时不自动弹窗**，改为 popover 内「启用精确定位」按钮按需请求（AXIsProcessTrustedWithOptions）。未授权时自动降级（鼠标记录/plist 估算）。
- **签名**：自签名证书「DockPopover Dev」（.keys/dp.keychain，密码 dpdev，keyUsage=digitalSignature+codeSigning 必须）。codesign 需 unlock-keychain。
- 构建：`Scripts/build_app.sh`（需 --disable-sandbox，SPM 沙盒在本机不可用）。产物 `dist/DockPopover.app`。

## 工程约定
- 单文件 ≤ 300 行，类型定义集中管理，测试紧邻源文件。
- 代码注释用中文；UI 文案中文。
